apiVersion: v1
kind: Service
metadata:
  name: rabbit1
  namespace: distributed-rabbit
spec:
  selector:
    app: rabbit1
  ports:
    - name: amqp
      port: 5672
      targetPort: amqp
    - name: management
      port: 15672
      targetPort: management
  type: ClusterIP
